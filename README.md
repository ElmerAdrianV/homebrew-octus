# Octus CLI - Distribution / Distribución

[English](#english) | [Español](#español)

---

<a name="english"></a>
## 🇬🇧 English

This repository hosts the binaries and installation formulas for **Octus CLI**.

### 🚀 Installation

#### macOS & Linux (Homebrew)

The recommended way for macOS and Linux users is via Homebrew.

```bash
brew tap ElmerAdrianV/octus
brew install octus
```

To upgrade to a new version:
```bash
brew upgrade octus
```

#### macOS & Linux (Direct Script)

If you don't use Homebrew, you can use our automated installation script:

```bash
curl -fsSL https://raw.githubusercontent.com/ElmerAdrianV/homebrew-octus/main/install/install.sh | bash
```

#### Windows (PowerShell)

To install on Windows, open PowerShell and run:

```powershell
irm https://raw.githubusercontent.com/ElmerAdrianV/homebrew-octus/main/install/install.ps1 | iex
```

This will download the executable, place it in `%LOCALAPPDATA%\Octus\bin`, and add that path to your PATH environment variable.

---

<a name="español"></a>
## 🇪🇸 Español

Este repositorio contiene los binarios y fórmulas de instalación para **Octus CLI**.

### 🚀 Instalación

#### macOS y Linux (Homebrew)

La forma recomendada para usuarios de macOS y Linux es usar Homebrew.

```bash
brew tap ElmerAdrianV/octus
brew install octus
```

Para actualizar a una nueva versión:
```bash
brew upgrade octus
```

#### macOS y Linux (Script Directo)

Si no usas Homebrew, puedes usar nuestro script de instalación automática:

```bash
curl -fsSL https://raw.githubusercontent.com/ElmerAdrianV/homebrew-octus/main/install/install.sh | bash
```

#### Windows (PowerShell)

Para instalar en Windows, abre PowerShell y ejecuta:

```powershell
irm https://raw.githubusercontent.com/ElmerAdrianV/homebrew-octus/main/install/install.ps1 | iex
```

Esto descargará el ejecutable, lo colocará en `%LOCALAPPDATA%\Octus\bin` y agregará esa ruta a tu PATH.

---

## 📦 Manual Downloads / Descargas Manuales

If you prefer manual downloads, visit the [Releases](https://github.com/ElmerAdrianV/homebrew-octus/releases) section.

Si prefieres descargar los ejecutables manualmente, visita la sección de [Releases](https://github.com/ElmerAdrianV/homebrew-octus/releases).

